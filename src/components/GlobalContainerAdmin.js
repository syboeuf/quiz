import Container from "@mui/material/Container";

const GlobalContainerAdmin = ({ children }) => (
  <Container sx={{ pt: 4, pb: 4, height: "100%" }}>{children}</Container>
);

export default GlobalContainerAdmin;
